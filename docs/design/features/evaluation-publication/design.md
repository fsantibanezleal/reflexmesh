# evaluation-publication design

Frozen installed-wheel lineage binds source, native binary, dependencies, checkpoints and workload specifications. Report core, structural and upstream benchmarks separately. Package deterministic model assets outside the wheel. Publication uses portable wheel builds and scoped GitHub OIDC trust.

The product SDD defines the scope and deploy boundary. Requirements refer to executable gates and are not inferred from successful rendering or method names.
